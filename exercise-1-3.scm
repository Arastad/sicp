(define (square a)
  (* a a)
)

(define (sum-of-the-squares-largest a b c)
  (if (>= a b)
      (+ (square a) (if (>= b c)
	  (square b)
	  (square c))
      )
      (+ (square b) (if (>= a c)
	  (square a)
	  (square c))
      )
   )
)
